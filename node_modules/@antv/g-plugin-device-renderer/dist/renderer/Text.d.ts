import type { DisplayObject } from '@antv/g-lite';
import type { Instanced } from '../meshes';
import { TextMesh } from '../meshes';
import type { RenderInst } from '../render/RenderInst';
import { Batch } from './Batch';
export declare class TextRenderer extends Batch {
    /**
     * one for fill, one for stroke
     */
    meshes: (typeof TextMesh)[];
    shouldSubmitRenderInst(object: DisplayObject, index: number): boolean;
    beforeUploadUBO(renderInst: RenderInst, mesh: Instanced, index: number): void;
}
