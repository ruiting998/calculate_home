import type { DisplayObject } from '@antv/g-lite';
import { InstancedPathMesh, SDFMesh } from '../meshes';
import { Batch } from './Batch';
/**
 * Use 2 meshes:
 * * SDF to draw fill & simple stroke if needed.
 * * InstancedPathMesh to draw stroke separately.
 */
export declare class CircleRenderer extends Batch {
    meshes: (typeof SDFMesh | typeof InstancedPathMesh)[];
    shouldSubmitRenderInst(object: DisplayObject, index: number): boolean;
    /**
     * need an additional mesh to draw stroke:
     * 1. strokeOpacity < 1
     * 2. lineDash used
     * 3. stroke is not 'none'
     */
    private needDrawStrokeSeparately;
}
