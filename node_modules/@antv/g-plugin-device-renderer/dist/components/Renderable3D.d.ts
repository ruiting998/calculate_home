import type { Instanced } from '../meshes';
export declare class Renderable3D {
    static tag: string;
    pickingId: number;
    encodedPickingColor: [number, number, number];
    meshes: Instanced[];
}
