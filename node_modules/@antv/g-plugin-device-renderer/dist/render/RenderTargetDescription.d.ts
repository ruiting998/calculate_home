import type { Color, Format } from '../platform';
export declare class RGRenderTargetDescription {
    pixelFormat: Format;
    width: number;
    height: number;
    sampleCount: number;
    colorClearColor: Readonly<Color> | 'load';
    depthClearValue: number | 'load';
    stencilClearValue: number | 'load';
    constructor(pixelFormat: Format);
    /**
     * Set the dimensions of a render target description.
     */
    setDimensions(width: number, height: number, sampleCount: number): void;
    copyDimensions(desc: Readonly<RGRenderTargetDescription>): void;
}
