import { CanvasContext, CSSGradientValue, DisplayObject, Pattern } from '@antv/g-lite';
import type { Device, Texture, TextureDescriptor } from './platform';
export interface GradientParams {
    width: number;
    height: number;
    gradients: CSSGradientValue[];
    instance: DisplayObject;
}
export declare class TexturePool {
    context: CanvasContext;
    constructor(context: CanvasContext);
    private textureCache;
    getOrCreateTexture(device: Device, src: string | TexImageSource, descriptor?: TextureDescriptor, successCallback?: (t: Texture) => void): Texture;
    getOrCreateCanvas(): import("@antv/g-lite").CanvasLike;
    getOrCreateGradient(params: GradientParams): void;
    getOrCreatePattern(pattern: Pattern, instance: DisplayObject, callback: () => void): void;
    destroy(): void;
}
